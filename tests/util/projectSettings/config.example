# TEST A COMMENT
dim = 3
DIM = 2 # NOTE: these parameters are CASE SeNSitIVE
# Test precision of convert to double
EPSILON = 1.23456789012 
# We can write parameters as floats instead of double:
RADIUS = 1.23456f
RADIUS = 1.23456e0f # And use more complex notation
# Strings are NOT \" delimited
CVT_FILENAME = cvt_circle.txt
# no newline at eof required?
